---
// Type definitions
type Props = {
  content: string
  class?: string
}
// Libraries imports
import { twMerge } from 'tailwind-merge'
// Props
const { content, class: AstroClass } = Astro.props
---

<h2 class={twMerge('text-xl text-secondary_text', AstroClass)}>{content}</h2>
