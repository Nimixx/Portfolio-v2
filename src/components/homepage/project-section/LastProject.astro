---
import { Image } from "astro:assets";
import { project } from "@components/hooks/fetchProject";
import FormattedDate from "@src/components/ui/FormattedDate.astro";
import Sections from "@src/layouts/Sections.astro";
const slicedPosts = project.slice(0, 6);
---

<Sections>
  <h2 class="text-4xl font-medium">Poslední projekty</h2>
  <p class="text-xl text-gray-400">
    Pravidelně se snažím psát články o tom co mě zajímá a co mě baví. Většinou
    se jedná návody na různé technologie a věci kolem vývoje moderních webových
    aplikací. Někdy píšu moje názory na nejnovější trendy a technologie.
  </p>
  <ul class="w-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 pt-2">
    {
      slicedPosts.map((project) => (
        <li class="w-full border border-primary_gray/40 p-4 rounded-2xl hover:bg-primary_gray/10 transition-colors animate-in fade-in duration-1000">
          <a href={`/project/${project.slug}/`} class="flex flex-col gap-4">
            <section class="w-full h-80 rounded-2xl overflow-hidden relative">
              <Image
                format="avif"
                quality="low"
                width={1280}
                height={720}
                src={project.data.heroImage}
                alt=""
                class="w-full h-full object-cover"
              />
            </section>
            <article class="flex flex-col gap-2">
              <p class="text-center text-gray-400">
                <FormattedDate date={project.data.pubDate} />
              </p>
              <h4 class="text-center text-2xl font-semibold ">{project.data.title}</h4>
            </article>
          </a>
        </li>
      ))
    }
  </ul>
</Sections>
